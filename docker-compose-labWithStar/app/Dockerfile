# Указываем базовый образ
FROM node:latest

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /usr/src/app

# Копируем файлы из директории app в рабочую директорию контейнера
COPY ./app/ .

# Устанавливаем зависимости
RUN npm install

# Запускаем приложение
CMD ["node", "index.js"]

